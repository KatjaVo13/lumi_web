# Build

## Standard build command
`npm run build`

## Expected outcome
- the project packages or compiles as expected for its stack
- build-time validation catches obvious drift
- no newly introduced warnings are ignored without reason

## Notes
Production build must succeed with AOT and strict TypeScript settings where possible.
