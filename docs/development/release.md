# Release

## Principle
Release planning must reflect cross-repo compatibility, not just local repository readiness.

## Repository notes
Web UI can usually be deployed after backend is compatible. Coordinate release when backend introduces required fields or breaking auth/session behavior.
