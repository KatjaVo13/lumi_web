# DEPENDENCIES

## Provides
Consumes backend HTTP API and shared auth assumptions. Provides feedback on browser usability and DTO fit.

## Consumes
- project-wide product decisions and contract definitions from sibling repositories
- CI expectations and release sequencing from the real project setup

## Coordination note
When dependencies change in a way that affects another repository, update this file together with `CROSS-REPO.md` and `HANDOFF.md`.
