# Integration boundaries

## Principle
Each repo must know what it consumes, what it provides, and which assumptions belong elsewhere.

## Notes
Consumes backend HTTP APIs. It should treat auth, error envelopes, server timestamps, and pagination semantics exactly as defined by backend.
